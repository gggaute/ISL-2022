import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  cardHeader: {
    padding: '12px',
  },
  card: {
    marginTop: '3px',
    marginRight: "4px",
    marginLeft: "4px",
  },
  link: {
    textDecoration: 'none',
  }
}));
export default useStyles;